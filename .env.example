# Local only — copy to .env or use Spring Boot env / IDE run config. Never commit real secrets.

# SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5432/fostertogether
# SPRING_DATASOURCE_USERNAME=
# SPRING_DATASOURCE_PASSWORD=

# Cognito resource server (issuer URI from AWS console)
# SPRING_SECURITY_OAUTH2_RESOURCESERVER_JWT_ISSUER_URI=https://cognito-idp.us-east-1.amazonaws.com/us-east-1_xxxx

COGNITO_ISSUER_URI=
AWS_DEV_ACCOUNT_ID=
AWS_PROD_ACCOUNT_ID=